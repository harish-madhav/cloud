npm install firebase

firebase login
firebase init
	if another proj is there, firebase use --add 
		select proj
    firestore setup -> default and yess
    hosting setup ->public
	                single-page app -> yes
	                others -> no
firebase deploy
